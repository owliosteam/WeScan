# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'WeScan' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for WeScan
  pod 'Pickle', '1.3.1'

  target 'WeScanTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'WeScanSampleProject' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for WeScanSampleProject

end
